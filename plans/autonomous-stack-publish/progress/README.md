# progress/

One file per task, copied from `_template.md` and named `{task-id}.md`. `/execute` writes these as it runs each task. `tasks.json` is the source of truth for status; these files carry the human-readable trail (validation output, blockers, notes).
