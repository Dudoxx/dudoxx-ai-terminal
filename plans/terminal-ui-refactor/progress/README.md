# Progress — terminal-ui-refactor

`/execute` writes one `{task_id}.md` here per task (copy `_template.md`). The
`tasks/` folder is the source of truth for topology + task nodes; this folder
tracks execution state per task. Do not hand-author beyond the template.
