# Task 001: Add bounded snapshot/scrollback frame to @ddx/term-contract

- **Status**: done
- **Agent**: ddx-term-broker-specialist
- **Started**: 2026-07-14
- **Completed**: 2026-07-14

## Steps done
- [x] Read `packages/ddx-term-contract/src/ws-frames.ts` fully; confirmed FrameBase extend pattern and the two union registration points.
- [x] Added `SnapshotFrameSchema = FrameBase.extend({ type: z.literal('snapshot'), data, withAnsi?, cols, rows, scrollbackLines? })` with JSDoc documenting cold-attach repaint purpose and the explicit `undefined scrollbackLines = viewport-only` semantics.
- [x] Registered `SnapshotFrameSchema` in `ServerFrameSchema` and `TermFrameSchema` discriminated unions (placed before `InputFrameSchema`, in the server→client group); added `'snapshot'` to `TERM_FRAME_TYPES` (before `'input'`).
- [x] Exported `type SnapshotFrame = z.infer<typeof SnapshotFrameSchema>`.
- [x] Updated `ws-frames.spec.ts`: added `snapshot` sample payload to the generic `sampleFrames` table (covered by existing "every variant parses + carries terminalId" and "rejects without terminalId" loop tests), plus a dedicated `describe('SnapshotFrame (cold-attach repaint)')` block asserting: `'snapshot'` is in `TERM_FRAME_TYPES`, a viewport-only parse (no `scrollbackLines`) succeeds with `scrollbackLines === undefined`, a parse with `scrollbackLines: 500` succeeds and round-trips the value, and a snapshot missing `terminalId` is rejected.
- [x] Ran contract build (esm+cjs+types) + test + typecheck — all green.

## Validation output
```
$ pnpm --filter @ddx/term-contract build
> build:esm (tsc -p tsconfig.esm.json) — OK
> build:cjs (tsc -p tsconfig.cjs.json) — OK
> build:types (tsc -p tsconfig.types.json) — OK

$ pnpm --filter @ddx/term-contract test
✓ src/mcp-tools.spec.ts (11 tests)
✓ src/ws-frames.spec.ts (13 tests)
Test Files  2 passed (2)
     Tests  24 passed (24)

$ pnpm --filter @ddx/term-contract typecheck
tsc --noEmit -p tsconfig.json — OK (no output)
```

## Notes / deviations
None. Implementation matches the task's `steps[]` verbatim — no schema fields added/removed beyond spec. `SnapshotFrameSchema` extends `FrameBase` so `terminalId` enforcement is structural, not ad hoc. Contract package builds first in the turbo DAG; no broker/mcp/web files touched (in scope for task 002/004).

All three acceptance criteria satisfied:
1. `SnapshotFrame` type/schema exported with terminalId + data + cols + rows + optional scrollbackLines — verified by build + typecheck.
2. Snapshot frame lacking terminalId rejected — verified by spec test.
3. `TERM_FRAME_TYPES` includes `'snapshot'` — verified by spec test.
