# Task 005: Toucan Signal design-token compliance (globals.css + i18n type escapes)

- **Status**: done
- **Agent**: ddx-term-web-specialist (impl-web-terminal)
- **Started**: 2026-07-14
- **Completed**: 2026-07-14

## Steps done
- [x] Read web-audit.md + globals.css:31-67 (primary/accent split, cobalt gap, dead xterm-hex block).
- [x] Unified `--color-primary`/`--color-accent` to ONE terracotta (`oklch(0.62 0.16 47)`, D-014 hue 47).
      Added `--color-link` (cobalt, `oklch(0.68 0.16 255)`) aliased by `--color-ring`/`--color-info`
      per §9a. `--color-success` left untouched (success-only).
- [x] Deleted the dead `--xterm-*-hex` mirror block (globals.css). Confirmed `appearance.ts`'s
      `TERM_THEMES` registry is untouched — it is the real xterm ITheme source (`XtermClient.buildTheme()`
      consumes it, not the CSS vars).
- [x] Declared Bricolage Grotesque via `next/font/google` in `[locale]/layout.tsx` (`--font-bricolage`
      CSS var, `display:'swap'`), exposed as `--font-display` in globals.css. Inter (`--font-sans`) +
      JetBrains Mono (`--font-mono`) unchanged.
- [x] Replaced both `t(\`...\` as never)` escapes in `AppearanceControls.tsx` with typed
      `FONT_LABEL_KEYS`/`THEME_LABEL_KEYS` const maps (`as const satisfies Record<string,string>`) +
      a `keyof` union cast on the registry's own `labelKey` field — zero `any`, zero `never`.
- [x] Grepped `src/` for raw palette utilities, gradients, raw hex/rgb/oklch outside appearance.ts,
      and container-border separators — all clean (see Validation output).
- [x] Ran typecheck/lint/test.

## Validation output

```
$ pnpm --filter ddx-term-web typecheck
src/lib/term/xterm-client.ts(368,15): error TS2322: ... is not assignable to type 'never'.
```
Pre-existing, out-of-scope failure — `xterm-client.ts` was NOT touched in this task's diff
(`git diff --stat` confirms zero changes to that file). This is task_004/task_001 territory
(the `@ddx/term-contract` snapshot-frame schema landing ahead of its consumer wiring). All
files actually in this task's scope (`globals.css`, `[locale]/layout.tsx`,
`AppearanceControls.tsx`) contain no type errors.

```
$ pnpm --filter ddx-term-web lint
> eslint src --fix
(no output — clean)
```

```
$ pnpm --filter ddx-term-web test
✓ src/lib/term/xterm-client.spec.ts (5 tests) 19ms
Test Files  1 passed (1)
     Tests  5 passed (5)
```

```
$ rg raw-palette-utilities / gradients / raw-hex-outside-appearance.ts / container-borders
(all zero hits — one comment-only "border" mention documenting compliance, not a violation)
```

## Notes / deviations

- **AC2.7 (light/dark theme toggle, xterm canvas intact) — VERIFIED-PENDING-BROWSER.** Cannot
  start a dev server in this sandbox per operator directive this session; typecheck+lint+test
  are the ceiling of static verification for a visual/theme AC. Operator + lead to run the
  Playwright/browser pass.
- Terracotta hue picked as `47` (D-014 canonical `0.55 0.14 47` from `dudoxx-branding/references/palette.md`
  §1) but lightness/chroma bumped to `0.62 0.16` to read correctly against this app's dark-first
  `--color-background: oklch(0.14 0.02 264)` — the canonical D-014 numbers are light-mode calibrated;
  this repo has no light theme (whole shell is `className="dark"` unconditionally, see layout.tsx).
  Hue angle (47) is preserved exactly per the "within ±2" AC; L/C adjusted for contrast on a near-black
  ground, which is standard OKLCH practice (same lightness-only adaptation pattern used for the D-014
  dark-mode clay/cobalt values in palette.md's own `.dark` block).
- `--color-link` is a NEW token not in the original `@theme` block — added additively per §9a's
  explicit requirement ("no `--color-link` token exists at all" was audit finding HIGH #2). No
  existing consumer referenced a `--color-link`-shaped gap before this, so no migration needed.
- `--color-info` merged into the same value as `--color-link` (cobalt) rather than kept as a
  separate near-duplicate hue, per §9a: "`--color-link` (= `--color-ring`/`--color-info`)" — these
  are meant to be one cobalt axis, not three separate colors.
- Bricolage Grotesque is declared but not yet applied to any specific heading element in this repo
  (no `font-display` Tailwind utility usage found in the 3 files touched) — the audit flagged the
  *absence of the token*, not a specific unstyled heading; `sidePanel.sessions` h2 mentioned in the
  audit lives in `TerminalSidePanel.tsx`, which is OUT OF SCOPE for task_005 (not in `files.modify`).
  Applying `font-display` to that heading is a follow-up, not silently done here (scope boundary).
