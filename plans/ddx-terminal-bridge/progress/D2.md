# D2 — Per-package CLAUDE.md (DOX) ×4 + root README + .mcp.json.example

Status: COMPLETED
Owner: docs-d2
Date: 2026-06-28

## Files created (6, per D2.files.create)
- `packages/ddx-term-contract/CLAUDE.md` — DOX (Purpose/Ownership/Local Contracts/Verification). Encodes "all shared zod types live HERE only" + terminalId-vs-pid distinction + dual ESM/CJS/types build contract.
- `ddx-term-broker/CLAUDE.md` — DOX. Encodes the tmux footguns verbatim: `-f /dev/null` (no config inheritance), `set-option -g default-size 120x30`, and NEVER `set-window-option -g window-size manual` (kills tmux 3.6a). Port 6481, per-terminalId WS fan-out, control-mode (`tmux -CC`) attach.
- `ddx-term-mcp/CLAUDE.md` — DOX. Carries the load-bearing invariant ("MUST NEVER hold a PTY (no node-pty, no pty.spawn, no child_process of a raw shell); shells out to tmux ONLY") + the send/capture mechanics (send-keys -l + SEPARATE Enter; snapshot = visible viewport `capture-pane -p` no `-S`; read = scrollback delta `-S -N` + per-terminal cursor) + full env table. Bin path confirmed `node dist/server.js` (ESM, plain node).
- `ddx-term-web/CLAUDE.md` — DOX. xterm.js, one WS per terminalId, tab-switch = WS resubscribe + `GET /api/v1/terminals/:id/snapshot` restore (not full reconnect), port 3460, Dudoxx frontend rules.
- `README.md` (root) — architecture one-liner, package table, one-command bring-up (`pnpm dev` via turbo = broker 6481 + web 3460; MCP launched over stdio by the client, built via `pnpm build`), the `.mcp.json` registration snippet + env table, and the native-attach recipe (`tmux -CC attach -t ddx-shared` from iTerm2/WezTerm).
- `.mcp.json.example` — registration snippet from MCP-SPEC §1 (command `node`, args `…/ddx-term-mcp/dist/server.js`, env DDX_TERM_SOCKET/DDX_TERM_SESSION/DDX_TERM_DEFAULT). Valid JSON.

## Validation (exit 0)
```
test -f README.md && test -f ddx-term-mcp/CLAUDE.md \
  && rg -q 'never own|no node-pty|tmux only|NEVER.*PTY' ddx-term-mcp/CLAUDE.md \
  && rg -q 'mcpServers' .mcp.json.example
→ VALIDATION_EXIT=0
```
Matched in ddx-term-mcp/CLAUDE.md: "MUST NEVER hold a PTY (no `node-pty`, …)".
Attribution line present on all 5 docs (README + 4 CLAUDE.md). `.mcp.json.example` is valid JSON (jq).

## Accuracy basis (read, not edited)
Each doc derived from the real package.json + entry/structure: contract `src/{terminal,session,ws-frames,mcp-tools}.ts` + dual-build tsconfigs; broker `main.ts` (port 6481, host 127.0.0.1), `modules/{session,terminal,control-mode,gateway}`, gateway `path:'/term'` per-terminalId; mcp `src/{server.ts,tools/term-*.tool.ts,tmux/tmux.client.ts,no-pty.spec.ts}` (ESM, bin → dist/server.js); web `app/[locale]/terminal/page.tsx` (tab resubscribe + snapshot fetch), `lib/term/xterm-client.ts`, port 3460.

## Boundaries respected
No product source touched (read-only to describe). No edits to ddx-cli-py/ddx-cli-ts, root config, e2e/, or plans/ (except this progress note).
