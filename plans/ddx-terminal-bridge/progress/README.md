# progress/

Per-task execution records, one file per `tasks.json` task id (e.g. `A1.md`).
`/execute` writes here as it works — copy `_template.md`, fill it, flip status.
The plan is NOT done until every task has a `done` progress file and `/unify` reconciles.
