# Progress — A1: Workspace scaffold

Status: COMPLETED · Agent: main-session (Direct-Implement prelude) · Date: 2026-06-28

## Files created
- `pnpm-workspace.yaml` — globs `packages/*` + 3 app dirs; ddx-cli-py/ddx-cli-ts EXCLUDED (fixtures, not deleted).
- `package.json` — name `dudoxx-ai-terminal`, private, pnpm@10.24.0, turbo scripts (dev/build/test/typecheck/lint), attribution.
- `turbo.json` — pipeline: build(^build), test(build), typecheck(^build), lint, dev(cache:false,persistent).
- `tsconfig.base.json` — strict, ES2022, moduleResolution Bundler, noUncheckedIndexedAccess.
- `docker-compose.dev.yml` — broker(6481) + web(6480) only; MCP is stdio, NOT a service.
- `.gitignore` — node_modules/dist/.next/.turbo, *.sock, findings/* policy.
- Empty package dirs + `.gitkeep`: packages/ddx-term-contract, ddx-term-broker, ddx-term-mcp, ddx-term-web.

## Validation
- `pnpm install` → exit 0 (root resolves clean).
- `turbo run build --dry` → turbo 2.10.0 parses workspace, 0 packages in scope (expected — package.json files owned by A2/B/C).

## Deviations
- A1's AC "enumerate exactly four packages" is only fully provable once A2 lands the first `package.json`
  (A1 creates empty dirs + .gitkeep only, per its own Boundaries — internals are A2/B/C scope). Workspace
  config is valid and turbo resolves; full enumeration deferred to A2's install, which is the correct gate.

## Invariants honored
- No node-pty at root.
- Fixtures (ddx-cli-py, ddx-cli-ts) excluded from globs and untouched.
