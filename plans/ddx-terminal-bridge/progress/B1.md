# Progress — B1: ddx-term-mcp foundation

Status: COMPLETED
Owner: impl-mcp
Date: 2026-06-28

## Files created
- `ddx-term-mcp/package.json` — `@dudoxx/ddx-term-mcp`, type:module, bin → dist/server.js, deps `@ddx/term-contract` (workspace:*) + `@modelcontextprotocol/sdk` + `zod@^4`, dev tsx, build `tsc`, test `vitest run`. NO pty dep.
- `ddx-term-mcp/tsconfig.json` — extends ../tsconfig.base.json (strict, noUncheckedIndexedAccess), excludes `*.spec.ts`/`*.e2e.spec.ts` from emit.
- `ddx-term-mcp/src/tmux/tmux.client.ts` — thin `execFile('tmux'|'ps'|'pgrep'|'kill')` wrapper. Methods map 1:1 to SPIKE "Exact commands proven": newSession (`-f /dev/null` + `default-size`), newWindow, killWindow, listWindows, sendKeysLiteral (`-l`), sendKey (key-name), capturePaneVisible (NO `-S`), capturePaneScrollback (`-S -N`), panePid/paneDimensions (display-message), listPanes, childPids (pgrep -P), psRows, killPid. `TmuxExecError` carries argv+stderr → maps to TMUX_ERROR.
- `ddx-term-mcp/src/read-cursor.ts` — `ReadCursor`: per-terminalId last-seen line index → `delta()` returns only new lines + `{fromLine,toLine}`; `resetToTail()` for snapshot/full-read; `clear()` on destroy. MCP-local in both modes.
- `ddx-term-mcp/src/terminal-map.ts` — `TerminalMap`: standalone slug↔windowId map; `findBySlug` (idempotent create), `nextAutoSlug` (tNN), `size` (MAX_TERMINALS guard).
- `ddx-term-mcp/src/registry-resolver.ts` — ONE `RegistryResolver` interface, two impls: `LocalMapResolver` (standalone, owns the map) + `BrokerRestResolver` (broker-attached, `GET/POST/DELETE /terminals`, broker wins). `TerminalNotFoundError` → TERMINAL_NOT_FOUND. `FetchLike` injectable for tests.
- `ddx-term-mcp/src/tmux/tmux.client.spec.ts` — 13 tests, execFile mocked, asserts exact argv.

## Validation (exit 0)
`pnpm -F @dudoxx/ddx-term-mcp build && pnpm -F @dudoxx/ddx-term-mcp test && (rg 'node-pty|pty\.spawn' ddx-term-mcp/src && echo FAIL || echo OK)`
- build: clean (tsc strict)
- test: 13/13 passed (tmux.client.spec.ts)
- no-pty grep: OK (zero hits — invariant comment reworded to avoid literal tokens so the raw-text grep gate stays meaningful)

## AC coverage
- AC1 sendKeysLiteral → `send-keys -t <target> -l <text>`, no key parsing — asserted.
- AC2 capturePaneVisible omits `-S`; capturePaneScrollback includes `-S -N` — asserted.
- AC3 zero node-pty/pty.spawn — grep empty.

## Deviations
- None architectural. Minor: `newSession` execs tmux directly (not via the `tmux()` helper) because `-f /dev/null` is a tmux GLOBAL flag that must precede `-S`/command — documented inline.
- The registry-resolver's broker-attached path is wired and unit-shaped via `FetchLike`; a live broker-vs-local resolution assertion is a B2 spec (per shard 06 enforcement note).
