/**
 * postcss.config.mjs — Tailwind v4 PostCSS plugin for Next.js 16.
 *
 * Dudoxx UG / Acceleate Consulting - Walid Boudabbous <walid@acceleate.com>
 */

/** @type {import('postcss').ProcessOptions} */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
