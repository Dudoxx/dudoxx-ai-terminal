# PRIMER — dudoxx-ai-terminal — 2026-06-28 14:51

Resume context for the next session (read by `/prime handoff:...`).

- **Last commit**: `27b9268` (pushed, tree clean). Docs + harness bootstrap session.
- **Project**: shared multi-terminal bridge — one tmux session, human (web/native) + AI
  agent (MCP) attach to the SAME terminals. 4 workspace packages + 2 CLI fixtures.
- **Harness is now live**: root `CLAUDE.md` + `.claude/agents/ddx-term-{broker,mcp,web}-specialist.md`
  + `context/{_invariants,REMINDERS,PROGRESS}.md`. Read `context/_invariants.md` before any edit.
- **Docs**: `ddx-documentation/README.md` is the index. `CLAUDE_{ARCHITECTURE,PACKAGES,PUBLISHING}.md`
  are load-on-demand deep-dives.
- **Two facts not to regress**: (1) MCP publish IS wired (tsup bundles the contract; NPM_TOKEN set).
  (2) Broker's node-pty is correct (tmux -CC attach), NO-PTY invariant is MCP-only.
- **Most likely next task**: first npm release via `pnpm changeset` → Version PR → publish.
- **Active plan**: `plans/ddx-terminal-bridge/` is RECONCILED (closed) — nothing pending there.
- **Verify before commit**: `pnpm typecheck && pnpm lint && pnpm test`. Never `pnpm dev` in-agent.

---
Dudoxx UG / Acceleate Consulting - Walid Boudabbous <walid@acceleate.com>
