# Handoff — 2026-07-10 01-42
## Next Action
**Trigger a fresh `ddx-term` MCP spawn (any term_* call, or restart Claude Code) to confirm the harness now runs the published 0.2.2 in-process — the disk artifact is 0.2.2 but the old MCP process was reaped this session.**
## Blockers
None. Stack is DOWN by design (reaped after validation); harness respawns 0.2.2 on next term_* call.
## Active Plans
- plans/autonomous-stack-publish, plans/ddx-terminal-bridge — prior sessions, not touched (no unreconciled COMPLETED this session).
## Pitfalls & Open Reminders
- ddx-term unit tests inject deps → GREEN while REAL bundle launch broke; ALWAYS production-test `node dist/server.js` before publish (spad-20260710-be8379).
- MCP `term_create` STACK_LAUNCH_FAILED stale-lock reclaim-throw (ensure-stack.ts:215) still throws instead of re-spawning — retriable, unfixed.
- Untracked `scratchpad/` stray in project root — decide .gitignore or remove.
## Context Needed
- ddx-term-mcp/src/supervisor/paths.ts (resolveBundleDir) + resolver-factory.ts (/api/v1 prefix) + scripts/build-stack.mjs (type:module).
- Production-test recipe: `( cat init+create.jsonl; sleep 55 ) | NODE_ENV=production node ddx-term-mcp/dist/server.js`; persistent = FIFO holder.
- Registered MCP: `node .../ddx-term-mcp/dist/server.js` (~/.claude/.claude.json).
## Reusable Session Context
- Workspace: dudoxxmac dev. Stack: NestJS broker :13330 + Next.js web :13340 + tmux ddx-shared, supervised by the MCP.
- Specialists: ddx-term-mcp-specialist / ddx-term-broker-specialist / ddx-term-web-specialist.
## Next Session Tasks
- [ ] Fix ensure-stack.ts reclaim-throw race (re-spawn, not STACK_LAUNCH_FAILED) → likely 0.2.3.
- [ ] Decide fate of untracked scratchpad/ dir.
## Completed
Fixed + published 0.2.1 (PTY-leak retry cap) then 0.2.2 (3 production-only stack-launch bugs); full browser-loop + interactive Python/HTTP validation via the real production MCP.
## Dead Ends
- Unit-test-green ≠ working: dep-injected tests hid all 3 bundle-launch bugs.
- `mcp__ddx-term__*` MCP tools stay disconnected after reap until harness reconnect — can't force from a tool call.
