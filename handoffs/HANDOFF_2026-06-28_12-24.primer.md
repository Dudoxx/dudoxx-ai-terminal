# Handoff — 2026-06-28 12:25
## Next Action
**Fix the BLANK web pane (0.A): agent-created terminals show tabs + "Connected" but render NO output, though term_read/term_snapshot return correct content. Debug the web render path — tab-select snapshot paint + live %output subscribe in xterm-client.ts/page.tsx. Then fix typing (0.B) + non-atomic term_destroy (0.C). Full detail: HANDOFF_2026-06-28_12-24.md §0.**
## Blockers
- 0.A web pane BLANK (primary, user-facing) — render path, not data; data exists in tmux.
- 0.B can't type — gateway sends browser `\r` as `send-keys -l` literal, never Enter (term.gateway.ts:255).
- 0.C term_destroy 500 → orphan registry entry (tmux killed, registry stale). 0.D registry↔tmux drift.
## Active Plans
- plans/ddx-terminal-bridge — COMPLETED, NOT reconciled (no SUMMARY.md) → `/unify` AFTER bug fixes.
## Context Needed
- ddx-term-web/src/lib/term/xterm-client.ts (sendInput:266, restoreSnapshot/onData, render path)
- ddx-term-web/src/app/[locale]/terminal/page.tsx (switchTo — snapshot fetch+paint on tab select)
- ddx-term-broker/src/modules/gateway/term.gateway.ts:255 (handleInputFrame Enter bug) + terminal.service.ts (destroy)
- Broker+web MUST be Node 22; services session-scoped (restart needed). MCP env: DDX_TERM_BROKER_URL=http://localhost:6481/api/v1
## Reusable Session Context
- dev/localhost; Node 22 (node-pty); NestJS11 + Next16 + zod/v4 + tmux. Broker: `cd ddx-term-broker && node dist/main.js`; web: `cd ddx-term-web && node server.mjs`.
- AGENT/MCP channel is SOLID (10/11 tools clean in 12:25 demo). Gaps are WEB-RENDER + destroy/registry only.
## Next Session Tasks
- [ ] 0.A blank pane → debug web render (snapshot paint + live frame subscribe); compare browser-created vs agent-created terminal.
- [ ] 0.B gateway Enter for per-char input; 0.C idempotent term_destroy; 0.D boot reconcile.
- [ ] /unify; rotate npm token.
## Completed
@dudoxx/ddx-term-mcp@0.1.3 live; 8+ chained infra bugs fixed (node-pty/Node22, raw-ws upgrade, pane→window resolver, Next single-origin WS, MCP↔broker registry, active default). Agent channel fully working.
## Dead Ends
- node-pty won't build on Node 26 → broker Node 22 (don't retry 26).
- tmux -CC from non-TTY fails (tcgetattr) → node-pty required, not `script`.
- npm strips data-URI/relative README imgs → raw.githubusercontent only (repo public).
- Test web render IN THE BROWSER — node ws client passed but browser pane is blank; they diverge.
