# Session Handoff — 2026-07-10 01-42
> Primer: HANDOFF_2026-07-10_01-42.primer.md (loaded by /prime)

## Completed This Session
- **Fixed the CRITICAL PTY-leak (handoff ISSUE 0)** in `ddx-term-broker/src/modules/control-mode/control-mode.attach.ts`: replaced the uncapped 2s-forever reconnect with a centralized `scheduleReconnect()` — `RECONNECT_MAX_ATTEMPTS=60`, exponential backoff (2s→30s), resets on healthy data, stops cleanly on give-up. 2 call sites (catch + onExit) route through it.
- **Browser-loop tested the stack** (Playwright MCP vs live :13340): keystroke echo (`echo DDX_LOOP_OK_42`→`DDX_LOOP_OK_42`), shared-state parity (browser DOM == tmux capture-pane == broker REST snapshot, byte-identical), multi-terminal black-pane (ISSUE 1) **does NOT reproduce** on baseline b33586c — Terminal 2 rendered + ran `echo PANE2_OK_$(uname -m)`→`PANE2_OK_arm64`.
- **Published 0.2.1** (PTY-leak fix + native pty.node in bundle) via changesets.
- **Production-mode validation surfaced 3 MORE bugs** (all unit-test-invisible) → fixed → **published 0.2.2**:
  1. `paths.ts` `resolveBundleDir` — tsup flattens to `dist/server.js`, old `resolve(dirname,'..')` overshot to package root → spawned nonexistent `<root>/broker/main.js`. Now probes sibling `broker/main.js`, falls back to parent.
  2. `resolver-factory.ts` — BrokerRestResolver called `/terminals`, broker mounts under `setGlobalPrefix('api/v1')` → 404. Now appends `/api/v1` once.
  3. `build-stack.mjs` — `dist/package.json` missing `type:module` → MODULE_TYPELESS reparse. Now written (broker keeps `dist/broker/package.json` type:commonjs override).
- **Full production interactive validation** via `node dist/server.js` over JSON-RPC: prod broker+web boot from correct dist/ paths, `term_create` succeeds, interactive `python3` REPL (`print('DDX_PY_OK',6*7)`→`DDX_PY_OK 42`), and `python3 -m http.server 18080` served `curl` HTTP 200 with `GET /` logged in the shared terminal. Leak-free (broker = 1 pty; count returned to baseline on reap).

## Key Decisions
- **Version bumps as patches** (0.2.1, 0.2.2) — all bug fixes, no public API change to the 10 term_* verbs.
- **changesets publish** (not bare npm publish) — respects the ignore-list (contract/broker/web stay private; only @dudoxx/ddx-term-mcp ships).
- **Fixed REST prefix at the factory** (buildResolver) not per-call-site — all 4 REST calls inherit the prefix; Cardinal #5/#20 (fix the seam).
- **Reaped stale MCP processes** (4 orphans, oldest Jul 2) + dev broker + tmux + locks so the next spawn boots clean 0.2.2. Precisely targeted ddx-term only — ddx-saas nest watch preserved.

## Dead Ends
- Unit-test-green ≠ working: dep-injected ensure-stack/resolver tests hid all 3 bundle-launch bugs. Only a real production launch caught them.
- `mcp__ddx-term__*` tools stay disconnected after reaping their process until the harness reconnects — cannot force a respawn from inside a tool call.
- First FIFO-driven interactive attempt raced (`python3import sys...` concatenated) — needs `sleep`-gated sends + `term_wait_for` between steps.

## Blockers
None. The stack is intentionally DOWN (reaped post-validation). The harness respawns 0.2.2 on the next term_* call.

## Files Changed
- `ddx-term-broker/src/modules/control-mode/control-mode.attach.ts` — bounded reconnect
- `ddx-term-broker/CLAUDE.md` — bounded-reconnect contract (DOX pass)
- `ddx-term-mcp/src/supervisor/paths.ts` — resolveBundleDir sibling-probe
- `ddx-term-mcp/src/resolver-factory.ts` — /api/v1 prefix
- `ddx-term-mcp/scripts/build-stack.mjs` — dist type:module
- `ddx-term-mcp/CHANGELOG.md` + `package.json` — 0.2.2

## Next Action
**Trigger a fresh `ddx-term` MCP spawn (any term_* call, or restart Claude Code) to confirm the harness runs the published 0.2.2 in-process — disk is 0.2.2, but the old MCP process was reaped this session.**

## Context Needed
- Registered MCP: `node .../ddx-term-mcp/dist/server.js` in `~/.claude/.claude.json`.
- Broker global prefix: `app.setGlobalPrefix('api/v1')` (main.ts:69) — the contract behind fix #2.
- Production-test recipe: `( cat init+create.jsonl; sleep 55 ) | NODE_ENV=production node ddx-term-mcp/dist/server.js`; persistent interactive = FIFO with a `sleep` write-holder so stdin doesn't EOF.
- Rebuild: `pnpm -F @dudoxx/ddx-term-mcp build:bundle && build:stack`.

## Reusable Session Context
- Workspace: dudoxxmac dev (context/ENVIRONMENT.md). Stack: NestJS broker :13330 + Next.js web :13340 + tmux ddx-shared on /tmp/ddx-term.sock, all supervised by the MCP's ensureStack.
- The broker legitimately holds ONE pty (control-mode `tmux -CC attach`); the MCP holds NONE (no-pty.spec.ts).
- Specialists: ddx-term-mcp-specialist, ddx-term-broker-specialist, ddx-term-web-specialist.

## Next Session Tasks
- [ ] Fix `ensure-stack.ts` reclaim-throw race (ensure-stack.ts:215-222 throws STACK_LAUNCH_FAILED on stale-lock reclaim instead of re-spawning) → next patch 0.2.3.
- [ ] Decide fate of untracked `scratchpad/` dir in project root (.gitignore or remove).
- [ ] Optional: add a real production-launch smoke test to CI so bundle-layout bugs can't ship again.

## Memory Seeds
- Written: `terminal-mcp-production-launch-bugs.md` (the 3 bugs + production-test recipe) + updated `terminal-broker-needs-real-tty.md` (backgrounded shell DOES run the broker).
- Candidate: "compile-green ≠ working" — dep-injected unit tests can hide 100% of real-bundle launch bugs; a production-launch smoke test is mandatory before publishing a supervised-stack MCP. Evidence: 3 bugs shipped in 0.2.1 despite 64/64 tests green.
- scratchpad pitfall spad-20260710-be8379 records the same.

## Active Plans
- `plans/autonomous-stack-publish/` — prior session, not touched this session.
- `plans/ddx-terminal-bridge/` — prior session, not touched this session.
- No plan reached COMPLETED this session; no /unify owed.
