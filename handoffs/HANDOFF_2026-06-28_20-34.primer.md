# Handoff — 2026-06-28 20:34
## Next Action
**Commit this session via `/quick-git` (UI customization + e2e pty-leak fix); both servers still running.**
## Blockers
None. (pty pool was the saga — root cause was OTHER dev stacks, not ddx-term; see Dead Ends.)
## Active Plans
plans/ddx-terminal-bridge/ — RECONCILED (closed, nothing pending)
## Context Needed
- Broker UP: pid on :6481, session `ddx-shared` on /tmp/ddx-term.sock (control-mode attach)
- Web UP: :3460 (`/en/terminal`), WS proxy → 6481
- ddx-term MCP connected, attached to same `ddx-shared` (10 tools verified)
- ddx-term-web/src/lib/term/{appearance,settings-store}.ts — new appearance model
- ddx-term-web/CLAUDE.md — updated ownership map
## Reusable Session Context
- dev workspace; pnpm+turbo monorepo; broker MUST start from a real TTY (not agent sandbox)
- UI work = ddx-term-web-specialist; e2e/tmux = ddx-term-broker-specialist
## Next Session Tasks
- [ ] `/quick-git` the UI + e2e-leak changes (18 files, all tsc-clean)
- [ ] Optional: browser-verify the side-panel/font/theme UI live at :3460/en/terminal
- [ ] Optional: first npm release via `pnpm changeset` (still the standing backlog item)
## Completed
Terminal UI max-customization (side panel + font size + 8 color themes, localStorage); e2e tmux pty-leak fix (3 layers); started full stack; ran the 10-tool MCP demo end-to-end.
## Dead Ends
- Don't start the broker from the agent's Bash sandbox — no pty (`Device not configured`); user runs it.
- pty `out of pty devices` ≠ ddx-term's fault — it holds ~1 pty total; culprit was concurrent Next/Turbo stacks vs the 511 cap.
- Counting `/dev/ttys*` nodes is NOT a live-usage metric (they persist).
