# Handoff — 2026-07-10 00-32
## Next Action
**Fix ISSUE 0 FIRST (CRITICAL): bound the broker's ControlModeAttach retry loop — it retries `pty.spawn(tmux -CC attach)` every 2s FOREVER on failure, leaking PTYs until macOS exhausts ptmx_max=511 (Ghostty "cannot allocate pty", can't type/create). Add max-attempts + backoff (mirror WS RECONNECT_MAX_ATTEMPTS=60) AND fix the underlying node-pty spawn failure (spawn-helper/pty.node missing from dist/broker/node_modules). Do NOT restart the broker until bounded — it re-leaks PTYs.**
## Blockers
- BROKER STOPPED (PTY leak) — killed to stop the leak; do not restart until retry loop is capped.
- Multi-terminal black-pane (ISSUE 1): 2nd+ pane BLACK, backend alive. Client lifecycle bug, unfixed (naive `disposed` re-check REGRESSED to "can't type" — reverted to b33586c).
## Active Plans
None (no plans/ in this repo).
## Pitfalls & Open Reminders
- Unit tests mock Terminal/ResizeObserver → PASS while breaking live DOM/WS; only real-browser verifies.
- After any rebuild, browser MUST hard-reload (Cmd+Shift+R) or stale chunk hashes break typing/create.
- MCP `term_create` throws STACK_LAUNCH_FAILED reclaim-race even when broker healthy (server.js:736).
- `ensureWeb` silently no-ops when `dist/web` standalone bundle unbuilt → run `pnpm build:stack` first.
## Context Needed
- Running: broker :13330 (healthy), web :13340 (HTTP 200) — both manual `nohup node`, NOT MCP-supervised.
- ddx-term-mcp/dist/server.js:686-748 (ensureBroker/ensureWeb/pollUntilHealthy reclaim-throw).
- ddx-term-web/CLAUDE.md + ddx-term-mcp/CLAUDE.md (DOX chain — READ before editing, I skipped it).
- Rebuild: `cd ddx-term-mcp && pnpm build:stack`; restart web: `NODE_ENV=production node dist/web/.next/standalone/ddx-term-web/server.mjs`.
## Reusable Session Context
- Workspace: dudoxxmac dev. Stack: Next.js 16 web (13340) + NestJS broker (13330) + tmux `ddx-shared` on /tmp/ddx-term.sock.
- Validate: `pnpm --filter ddx-term-web typecheck|lint|test` — but tests DON'T catch render bugs; drive the browser.
## Next Session Tasks
- [ ] Browser-verified fix for multi-terminal black-pane (page.tsx switchTo race vs 2s poll re-render).
- [ ] Fix MCP reclaim-throw race (server.js:736) so term_create succeeds first-try when broker is healthy.
- [ ] `ensureWeb` should error loudly (not no-op) when standalone bundle missing.
## Completed
Repaired wedged MCP stack (killed 11-day tmux, cleared locks); built missing web bundle via build:stack; brought broker+web live. Reverted a bad front-end fix to clean baseline b33586c.
## Dead Ends
- `disposed` post-await re-check in connect() → regressed to "can't type"; reverted. Don't re-apply naively.
- `next dev` (server.mjs dev mode) → findPagesDir crash in standalone; use production bundle.
- LLM `claude -p` probes to enumerate skills/registry → unreliable; use interactive `/context`.
