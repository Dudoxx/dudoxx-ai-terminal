# HANDOFF — dudoxx-ai-terminal — 2026-06-28 14:51

> Session: audit + DDX Harness bootstrap + full documentation + publish/CI verification.
> Commit: `27b9268` (pushed to origin/main). Working tree clean.

## What this session did
1. **Full codebase audit** → `findings/2026-06-28_full-codebase-audit/` (6 shards).
   Repo indexed in codebase-memory-mcp (1427 nodes).
2. **DDX Harness bootstrap** (manual — bootstrapper script has a `tr` zsh bug producing
   malformed `dudoxx-ai--` double-dash agent names):
   - Root `CLAUDE.md` (thin DOX cascade pointer to per-package CLAUDE.md).
   - `context/_invariants.md`, `REMINDERS.md`, `PROGRESS.md`.
   - 3 clean-context specialists in `.claude/agents/`: `ddx-term-{broker,mcp,web}-specialist.md`
     (all no-fork, all have Cold Start sections; path-scoped via `paths:` frontmatter).
   - Agent memory sidecars in `context/agents/`, `.claude/settings.local.json`, `.claude/.gitignore`.
3. **`ddx-documentation/`** — 16 MD+YAML files for coders (architecture, glossary,
   getting-started, per-package, 10-tool MCP reference, dev/invariants/contributing, publishing).
4. **Root referencable docs** — `CLAUDE_ARCHITECTURE.md`, `CLAUDE_PACKAGES.md`,
   `CLAUDE_PUBLISHING.md`, `INSTALLATION.md`, `SETUP.md`, `DEPENDENCIES.md`, `CHANGELOG.md`.
5. **Publish/CI** — verified the pipeline is production-grade (not broken). Corrected the
   stale README "npx not available" note + added a Release & versioning section.

## Two load-bearing corrections (evidence-verified — do NOT regress)
- **MCP publish is wired.** `tsup.config.ts` (`noExternal: ['@ddx/term-contract']`) bundles
  the contract into a self-contained `dist/server.js`; `prepublishOnly` runs `build:bundle`.
  `npm pack --dry-run` → 7-file tarball, zero `workspace:*` leak. `NPM_TOKEN` secret is SET.
  The contract stays `private` + in changeset `ignore[]` by design.
- **Broker's `node-pty` is CORRECT** — it allocates a pty for `tmux -CC attach` (the SHARED
  session), not a private PTY. The NO-PTY invariant is scoped to the MCP only
  (`no-pty.spec.ts`). Don't "fix" the broker by removing node-pty.

## State
- Active plan `plans/ddx-terminal-bridge/` is RECONCILED (closed).
- No source code was changed this session — docs + harness markdown only.
- MCP version `0.1.3`, unpublished (no registry release yet).

## Next-session task seeds
1. **First npm release** (when ready): `pnpm changeset` (pick bump) → commit → merge → the
   "Version Packages" PR → merge it → CI publishes `@dudoxx/ddx-term-mcp`. After that,
   `npx -y @dudoxx/ddx-term-mcp` resolves — update README/INSTALLATION's "until released" caveats.
2. **Optional**: run the new specialists on a real change to validate them
   (`ddx-term-broker/mcp/web-specialist` are path-scoped + clean-context).
3. **Optional cleanup**: the bootstrapper script's `tr -d '_'` zsh bug (`tr: illegal option`)
   — if re-running `project-bootstrapper`, it mis-detects project name. Bootstrap was done
   manually this session to avoid it. Not blocking.

## Memory Seeds
- [PUBLISH] · DISCOVERY · MCP publish wired via tsup noExternal; README "npx not available" was stale.
- [BROKER] · RULE · Broker node-pty is for tmux -CC attach (shared session), not a private PTY.
- [HARNESS] · RULE · Root CLAUDE.md is a thin DOX pointer; per-package CLAUDE.md own local detail.

---
Dudoxx UG / Acceleate Consulting - Walid Boudabbous <walid@acceleate.com>
