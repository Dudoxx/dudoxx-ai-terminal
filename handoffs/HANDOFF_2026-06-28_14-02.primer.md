# Handoff — 2026-06-28 14:02
## Next Action
**Two forward goals (user-requested): (1) improve the terminal WEB UI, (2) add NEW MCP capabilities with auto-wiring through broker→tmux→web viewer. Start by fixing the two known blockers (0.C destroy, 0.D registry-reconcile) since they block clean demos, THEN build the new features. Full brief: HANDOFF_2026-06-28_14-02.md.**
## Blockers
- 0.C term_destroy not atomic: kills tmux window, broker DELETE 500s before registry evict → orphan entry. Fix: idempotent destroy (treat "window gone" as success) + evict-before-respond. `ddx-term-broker/src/modules/terminal/terminal.service.ts` destroy + session.service.
- 0.D registry↔tmux drift: broker registry is in-memory, lost on restart; tmux windows survive → `GET /terminals` stale/500. Fix: boot-time reconcile (list tmux windows, drop/adopt). Both confirmed in 2× demo runs.
## Active Plans
- plans/ddx-terminal-bridge — RECONCILED (SUMMARY.md written). No open plan; new work needs a fresh /plan-feature.
## Context Needed
- Wiring seam (add-a-capability path): packages/ddx-term-contract/src/{mcp-tools.ts,ws-frames.ts} → ddx-term-mcp/src/tools/term-*.tool.ts → ddx-term-broker/src/modules/terminal/terminal.{controller,service}.ts + gateway/term.gateway.ts → ddx-term-web/src/lib/term/xterm-client.ts + app/[locale]/terminal/page.tsx
- Stack: broker `cd ddx-term-broker && node dist/main.js` (6481) · web `cd ddx-term-web && node server.mjs` (3460). BOTH need Node 22 (nvm: `export NVM_DIR=$HOME/.config/nvm; . $NVM_DIR/nvm.sh; nvm use 22`).
- Web viewer is bare: ONE page, tab bar + status bar + xterm pane. No split, no resize UI, no scrollback search, no copy/paste affordance, no theme toggle — all UI-improvement candidates.
## Reusable Session Context
- dev/localhost; NestJS11 + Next16 + zod/v4 + tmux 3.6a; MCP is PTY-FREE (invariant), broker owns tmux + registry.
- Routing: broker/gateway → nestjs-backend-specialist or debugger; web UI → stack-specialist + ddx-nextjs-shell/visuals skills; contract = shared seam (MCP bundles it, change carefully).
## Next Session Tasks
- [ ] Fix 0.C + 0.D (broker destroy idempotency + boot reconcile) — unblocks clean demos.
- [ ] /plan-feature the web UI improvements (split panes, resize, scrollback search, copy, theme).
- [ ] /plan-feature new MCP verbs (e.g. term_resize, term_split, term_write_file, term_paste) + their contract→broker→web wiring.
## Completed
8 browser-verified fixes (blank pane, typing/Ctrl-C, octal-decode, staircase, refresh-state, live-echo CR, web-crash resilience) + plan reconciled + committed/pushed d1e3bc1.
## Dead Ends
- WebGL renderer: silent blank pane in embedded Chrome — REMOVED, DOM renderer pinned. Don't re-add.
- `send-keys -l` for browser input: can't do Ctrl-C/arrows/Enter — use `send-keys -H` (hex raw bytes).
- Disabling zsh plugins to fix live-echo: WRONG lead — real cause was stray CR from PTY ONLCR in control-mode line-splitter.
