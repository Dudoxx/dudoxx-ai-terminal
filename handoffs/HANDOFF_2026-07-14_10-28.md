# Session Handoff — 2026-07-14 10:28
> Primer: HANDOFF_2026-07-14_10-28.primer.md (loaded by /prime)

## Completed This Session
Full terminal-ui-refactor (job → plan → team-execute → review → unify → browser-verify):
- **@ddx/term-contract**: `SnapshotFrame` added to `ws-frames.ts` (+spec).
- **ddx-term-broker**: `term.gateway.ts` restore-on-attach (`pushInitialSnapshot` before live dispatch) + snapshot-ordering test; `terminal.service.ts` bounded scrollback (`SCROLLBACK_LINES=500`); spec `liveMessages()` filter (10 tests).
- **ddx-term-web**: 8 new §10 route files (`view/[terminalId]/{,edit,readonly}`, `list/{grid,list,timeline}`, `new`, index) + `TerminalWorkspace` (route-param keyed, no useState), `TerminalListView`, `TerminalBreadcrumb`; `xterm-client.ts` buffer-until-painted + snapshot consumer; `globals.css` Toucan Signal tokens; edit-mode `autoRename` wiring.
- **ddx-term-mcp**: `tmux.client.ts` error-un-swallow + DRY `execTmux`; `term-ps.tool.ts` full-tree `descendantPids`; `term-wait-for.tool.ts` doc.

## Key Decisions
- D1=YES bounded scrollback, D2=YES full §10 entity grammar, D3=document wait_for non-reset, D4=align term_ps depth (operator redline).
- Engine: team-execute (3 term specialists) — the tasks fan out cleanly by service.
- Frame-first reciprocal pattern: contract → producer → consumer, exhaustive-`never` switch forces consumer wiring.

## Dead Ends
- `NODE_ENV=development node server.mjs` → React wedged (custom server doesn't proxy `_next/webpack-hmr`); UI stuck "Connecting…" with zero app errors. Fix: `NODE_ENV=production`.
- Broker started via a plain backgrounded Bash call dies (needs `tmux -CC` pty / real TTY); use `nohup ... &`.
- Stale `.next` build silently 404s new routes — rebuild before browser-verify.

## Blockers
None.

## Files Changed
37 tracked modified + 47 untracked (10 web route/component files + test artifact PNG). Key: contract `ws-frames.ts`, broker `term.gateway.ts`/`terminal.service.ts`, web `TerminalWorkspace.tsx`/`xterm-client.ts`/`globals.css`, mcp `tmux.client.ts`.

## Next Action
**Run /quick-git to commit + push the RECONCILED, browser-verified refactor (Walid attribution). Delete/gitignore the ac16-deeplink-*.png test artifact first.**

## Context Needed
- Running services: broker 13330 (`nohup node dist/main.js`, pid ~52302), web 13340 (`NODE_ENV=production node server.mjs`).
- `plans/terminal-ui-refactor/SUMMARY.md` — reconciliation + per-AC browser evidence.
- Serve-mode + broker-TTY gotchas → memory `web-serve-prod-not-dev`, `terminal-broker-needs-real-tty`.

## Reusable Session Context
Workspace development; stack ts/nestjs/nextjs/python. Team-execute with named `impl-{broker,web,mcp}-terminal` teammates worked; watch the reap-ownership trap (a teammate reports the whole team's bg IDs).

## Next Session Tasks
- [ ] /quick-git (commit + push)
- [ ] Optional browser: AC3.3 rapid tab-switch + AC2.7 dark-mode toggle (not yet screenshotted)
- [ ] Optional: security MED — harden `term.gateway.ts` WS-entry terminalId cast (safeParse + close 1008)

## Memory Seeds
- `[[web-serve-prod-not-dev]]` (written) · `[[team-reap-verify-ownership]]` (written) · `[[plan-agent-cwd-drift]]` (written).
- Candidate: `TerminalDescriptor.windowId` is a branded STRING (tmux @N), never orderable — use `createdAt` for chrono sort (now in ddx-term-web/CLAUDE.md).
- Candidate: byte-fidelity restore confirmed (broker raw snapshot == rendered, `echo -n` concatenation not a bug).

## Active Plans
- plans/terminal-ui-refactor/_index.md — RECONCILED (SUMMARY.md written, banner parity clean).
